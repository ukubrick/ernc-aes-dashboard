# Plantilla de secrets para Streamlit Cloud.
# En Streamlit Cloud: Settings → Secrets → pegar el contenido real (sin .example).
# Localmente: usar .env en raíz del proyecto (gitignored).

SUPABASE_URL = "https://ozeubcqoxsihmmfpswoa.supabase.co"
SUPABASE_KEY = "sb_publishable_..."   # ← usar el anon/publishable key, NO el service_role
